# .bash_exports
# for remote hosts

export LESS='-M'
export MYNAME='Lewis Overton'

#put path here

##
# Paul Irish dotfiles for setting up work environment
# source: paulirish/dotfiles - GitHub
# Includes adaptations for Lewy MacBook
# Component: .bash_export (set some defaults)

##
# Choose a default editor from this list
#export EDITOR="vim -W"
#export EDITOR="subl -W"
export EDITOR="mate -W"

# Don’t clear the screen after quitting a manual page
export MANPAGER="less -X"

# Larger bash history (allow 32³ entries; default is 500)
export HISTSIZE=32768
export HISTFILESIZE=$HISTSIZE
export HISTCONTROL=ignoredups    #ignoredups:ignorespace
export HISTTIMEFORMAT='%F %T '
export HISTIGNORE=history:df:'ls:':cd:'cd -':pwd:exit:date:'* --help'

# timestamps for bash history. www.debian-administration.org/users/rossen/weblog/1
# saved for later analysis
